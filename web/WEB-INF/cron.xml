<?xml version="1.0" encoding="UTF-8"?>
<cronentries>

    <cron>
        <url>/_ah/sessioncleanup?clear</url>
        <description>Clean up sessions</description>
        <schedule>every 15 minutes</schedule>
    </cron>

    <cron>
        <url>/api/v1/admin/dailybreed</url>
        <description>every day, breed bunnies</description>
        <schedule>every 24 hours</schedule>
    </cron>

    <cron>
        <url>/api/v1/admin/dailystarve</url>
        <description>every day, starve bunnies</description>
        <schedule>every 24 hours</schedule>
    </cron>

    <cron>
        <url>/api/v1/admin/updatestore</url>
        <description>every hour, new bunnies in store</description>
        <schedule>every 1 hours</schedule>
    </cron>

</cronentries>